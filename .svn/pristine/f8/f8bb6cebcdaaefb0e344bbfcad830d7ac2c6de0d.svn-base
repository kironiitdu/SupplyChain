using DMSApi.Models;
using DMSApi.Models.IRepository;
using DMSApi.Models.Repository;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Net;
using System.Net.Http;
using System.Web;
using System.Web.Http;
using System.Web.Http.Cors;

namespace DMSApi.Controllers
{
    //[EnableCors(origins: "*", headers: "*", methods: "*")]
    public class WarehouseTypeController : ApiController
    {
         private IWarehouseTypeRepository warehousetypeRepository;

        //public WarehouseTypeController()
        //{
        //    this.warehousetypeRepository = new WarehouseTypeRepository();
        //}

        //public WarehouseTypeController(IWarehouseTypeRepository warehousetypeRepository)
        //{
        //    this.warehousetypeRepository = warehousetypeRepository;
        //}

        //public HttpResponseMessage GetAllWarehouseType()
        //{
        //    var ware = warehousetypeRepository.GetAllWarehouseType();
        //    var formatter = RequestFormat.JsonFormaterString();
        //    return Request.CreateResponse(HttpStatusCode.OK, ware, formatter);
        //}

        //[HttpPost]
        //public HttpResponseMessage Post([FromBody]Models.warehouse_type oWarehouseType)
        //{
        //    try
        //    {
               
        //                Models.warehouse_type insertWarehouseType = new Models.warehouse_type
        //                {
        //                    warehouse_type_name = oWarehouseType.warehouse_type_name,
        //                    company_id = oWarehouseType.company_id,
        //                    created_by = oWarehouseType.created_by,
        //                    created_date = oWarehouseType.created_date,
        //                    branch_id = 1
        //                };
        //                bool save_ware_type = warehousetypeRepository.InsertWarehouseType(insertWarehouseType);
        //                var formatter = RequestFormat.JsonFormaterString();
        //                return Request.CreateResponse(HttpStatusCode.OK, new Confirmation { output = "success", msg = "Warehouse Type save successfully" }, formatter);
        //            }

        //    catch (Exception ex)
        //    {
        //        var formatter = RequestFormat.JsonFormaterString();
        //        return Request.CreateResponse(HttpStatusCode.OK, new Confirmation { output = "error", msg = ex.ToString() }, formatter);
        //    }
        //}

    }
}